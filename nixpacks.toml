[phases.setup]
nixPkgs = ["nodejs-20_x", "python312", "gcc"]

[phases.install]
cmds = [
    "npm install",
    "pip install -r requirements.txt"
]

[phases.build]
cmds = ["echo 'Build phase complete'"]

[start]
cmd = "streamlit run app.py --server.port=$PORT --server.address=0.0.0.0"